# Doctor v2.2.0 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add 4 new checks (46 total), improve 3 existing CC-checks, create 2 new layer files.

**Architecture:** New checks go into existing -EXTRA files (SECURITY-EXTRA) and 2 new files (FOUNDATION-EXTRA, QUALITY-PROD). Existing checks get minor additions. All index files (CHECKLIST, MATURITY, SKILL, README, install.sh, Makefile) updated to reflect 46 checks.

**Tech Stack:** Markdown + Bash scripts. No code dependencies. Verification: `make check` + `make lines`.

---

### Task 1: Create FOUNDATION-EXTRA.md — check 1g DB migrations

**Files:**
- Create: `layers/FOUNDATION-EXTRA.md`
- Modify: `layers/FOUNDATION.md` (add cross-link at bottom)

**Step 1: Create `layers/FOUNDATION-EXTRA.md`**

Write new file with check 1g (DB migration tool). Follow existing patterns from other -EXTRA files:
- Header referencing base file
- Check 1g with `[quality]` tag
- `<!-- glossary: ... -->` comment
- Checklist items (alembic/prisma/knex/typeorm detection)
- Bash script detecting DB presence first (only runs if DATABASE_URL / asyncpg / prisma found)
- Detection: `alembic/`, `prisma/migrations/`, `migrations/`, config files
- WHY explanation + source link
- Target: ~60-80 lines

Key script logic:
```bash
# Only check if project has a database
db_detected=false
grep -rq "DATABASE_URL" .env.example .env 2>/dev/null && db_detected=true
# Check for DB libraries in source
src_dirs=""; for d in bot src app lib; do [ -d "$d" ] && src_dirs="$src_dirs $d"; done
if [ -n "$src_dirs" ]; then
  grep -rqE "asyncpg|psycopg|prisma|typeorm|sequelize|knex|sqlalchemy|mongoose|drizzle" "$src_dirs" 2>/dev/null && db_detected=true
fi
# Then check for migration tool presence
```

**Step 2: Add cross-link to FOUNDATION.md**

At the end of FOUNDATION.md (before the last blank line), add:
```
> Продвинутые проверки (1g миграции БД) → [FOUNDATION-EXTRA.md](FOUNDATION-EXTRA.md)
```

**Step 3: Verify line counts**

Run: `wc -l layers/FOUNDATION-EXTRA.md layers/FOUNDATION.md`
Expected: FOUNDATION-EXTRA < 100, FOUNDATION ≤ 400

---

### Task 2: Create QUALITY-PROD.md — check 2l Error monitoring

**Files:**
- Create: `layers/QUALITY-PROD.md`
- Modify: `layers/QUALITY-EXTRA.md` (add cross-link at bottom)

**Step 1: Create `layers/QUALITY-PROD.md`**

Write new file with check 2l (Error monitoring). Structure:
- Header: "Слой 2: Качество — production-мониторинг"
- Reference to QUALITY.md and QUALITY-EXTRA.md
- Check 2l with `[core]` tag, `<!-- glossary: ... -->`
- Checklist items: Sentry/LogRocket/Axiom/Datadog configured, DSN in env
- Bash script detecting monitoring setup:
  - Check package.json for `@sentry/node`, `@sentry/react`, `@sentry/nextjs`
  - Check requirements.txt/pyproject.toml for `sentry-sdk`
  - Check .env.example for `SENTRY_DSN`, `LOGROCKET_APP_ID`, `DATADOG_API_KEY`
  - Check for sentry config files: `sentry.client.config.*`, `sentry.server.config.*`, `.sentryclirc`
  - If nothing found: recommend Sentry (free tier covers solo projects)
- WHY explanation: "Без мониторинга узнаёшь об ошибках от юзеров, а не от системы"
- Source link: https://docs.sentry.io/
- Target: ~60-80 lines

**Step 2: Add cross-link to QUALITY-EXTRA.md**

At the end of QUALITY-EXTRA.md, add:
```
> Production-мониторинг (2l error monitoring) → [QUALITY-PROD.md](QUALITY-PROD.md)
```

**Step 3: Verify line counts**

Run: `wc -l layers/QUALITY-PROD.md layers/QUALITY-EXTRA.md`
Expected: QUALITY-PROD < 100, QUALITY-EXTRA ≤ 400

---

### Task 3: Add checks 0l + 0m to SECURITY-EXTRA.md

**Files:**
- Modify: `layers/SECURITY-EXTRA.md:167-184` (before incident response section)

**Step 1: Add check 0l — AI API cost protection**

Insert before "## Если секреты утекли" (line 169). Check 0l with `[core]` tag:
- `<!-- glossary: billing alerts = уведомления при превышении лимита расходов на AI API -->`
- Checklist: billing alerts set, max_tokens in API calls, dev/prod keys separated
- Bash script:
  - Detect AI API usage: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_AI_API_KEY` in .env.example
  - Grep source for API calls without max_tokens: `openai.chat.completions.create` without `max_tokens`
  - Check if same key name used without `_DEV`/`_PROD` suffix
- WHY: "Зацикленный агент без max_tokens = $500 за ночь. Billing alerts — единственная страховка."
- Source: https://platform.openai.com/docs/guides/rate-limits
- ~50-60 lines

**Step 2: Add check 0m — Backup strategy**

Insert after 0l, before incident response. Check 0m with `[advanced]` tag:
- Checklist: managed DB backup or backup script exists, git remote configured (ref 0a)
- Bash script:
  - Check for managed DB indicators: Railway, Supabase, RDS, PlanetScale in configs
  - Check for backup scripts: `*backup*`, `pg_dump`, `mongodump` in scripts/
  - Check crontab for backup jobs
- WHY: "Git бэкапит код, но не данные. Managed DB включает бэкапы автоматически."
- Source: https://docs.railway.com/guides/backups
- ~40-50 lines

**Step 3: Verify line count**

Run: `wc -l layers/SECURITY-EXTRA.md`
Expected: ≤ 400 (was 183, adding ~100 = ~283)

---

### Task 4: Improve check 3a in INTELLIGENCE.md — model field

**Files:**
- Modify: `layers/INTELLIGENCE.md:24-51` (agents check script)

**Step 1: Add model recommendation to agent listing**

In the agent listing loop (lines 24-51), after the `model:` output, add logic:
- If agent has `tools:` that are read-only (only Read, Grep, Glob, Bash) AND no `model:` field:
  ```
  echo "     💡 Read-only агент без model: — добавь model: haiku для экономии"
  ```
- If agent has `model: haiku`: show as optimization win

**Step 2: Verify line count**

Run: `wc -l layers/INTELLIGENCE.md`
Expected: ≤ 234 + 8 = ~242 (well under 400)

---

### Task 5: Improve check 5a in DX.md — allowed-tools + launch.json

**Files:**
- Modify: `layers/DX.md:18-44` (skills check script)

**Step 1: Add allowed-tools check to skill listing**

In the skill listing loop, after checking triggers and args, add:
- Check if skill has `allowed-tools:` in frontmatter
- If missing: `⚠️ /$name нет allowed-tools — наследует ВСЕ инструменты`
- If present: show count of tools

**Step 2: Add launch.json sub-check**

After the skills check section (around line 66), add a small sub-check:
```bash
# launch.json для Claude Preview:
if [ -f .claude/launch.json ]; then
  echo "  ✅ .claude/launch.json (Claude Preview)"
else
  # Only suggest if project has a web server
  if [ -f package.json ] && grep -qE '"(dev|start|serve)"' package.json 2>/dev/null; then
    echo "  🔵 Нет .claude/launch.json — Claude Preview не сможет запустить dev-сервер"
  fi
fi
```

**Step 3: Verify line count**

Run: `wc -l layers/DX.md`
Expected: ≤ 312 + 15 = ~327 (well under 400)

---

### Task 6: Update CHECKLIST.md — new checks + counts

**Files:**
- Modify: `CHECKLIST.md`

**Step 1: Update tag counts table (lines 16-23)**

Change:
- `[core]` count: 17 → 19
- `[quality]` count: 7 → 8
- `[advanced]` count: 6 → 7
- `[cc]` count: 12 (unchanged)
- Total: 42 → 46

**Step 2: Add new checks to Layer 0 section (line ~43)**

After existing 0k line, add:
```
- [ ] `[core]` AI API cost protection — billing alerts, max_tokens в вызовах, dev/prod ключи раздельные
- [ ] `[advanced]` Backup strategy — managed DB backup или backup script, git remote + данные
```

**Step 3: Add new check to Layer 1 section (line ~59)**

After existing 1f line, add:
```
- [ ] `[quality]` Миграции БД — alembic/prisma/knex настроен (только если проект имеет БД)
```

Update `(6 проверок)` → `(7 проверок)` in section header.
Update Layer 1 details link to include FOUNDATION-EXTRA.md.

**Step 4: Add new check to Layer 2 section (line ~83)**

After existing 2k line, add:
```
- [ ] `[core]` Error monitoring — Sentry/LogRocket/Axiom настроен, SENTRY_DSN в env
```

Update `(11 проверок)` → `(12 проверок)` in section header.
Add link to QUALITY-PROD.md.

**Step 5: Update Layer 0 count**

Update `(11 проверок)` → `(13 проверок)` in Layer 0 section header.

**Step 6: Update maturity table at bottom (lines 132-139)**

Change Starter from 17 → 19, Growing from 24 → 27, Mature from 30 → 34, Pro from 42 → 46.

---

### Task 7: Update MATURITY.md — new counts + weights

**Files:**
- Modify: `layers/MATURITY.md`

**Step 1: Update tag reference (line 109)**

Change: `(17 core + 7 quality + 6 advanced + 12 cc = 42)` → `(19 core + 8 quality + 7 advanced + 12 cc = 46)`

**Step 2: Update layer counts in weight table (lines 125-132)**

Update check counts:
- Security: (11) → (13)
- Foundation: (6) → (7)
- Quality: (11) → (12)
- Intelligence, Context, DX: unchanged

**Step 3: Update example counts in scoring section (lines 140-143)**

Update the Starter/Growing/Mature/Pro layer examples to reflect new counts per layer and tag.

---

### Task 8: Update SKILL.md — description + counts

**Files:**
- Modify: `SKILL.md`

**Step 1: Update description (lines 4-5)**

Change "42 checks" → "46 checks" in the description field.

**Step 2: Update quick mode counts (line 205)**

If quick mode references "17 штук" for core checks, update to "19 штук".

**Step 3: Update maturity levels line (line 109)**

Change `Starter 🌱 (17 чеков), Growing 🌿 (24), Mature 🌳 (30), Pro ⚡ (42)` → `Starter 🌱 (19 чеков), Growing 🌿 (27), Mature 🌳 (34), Pro ⚡ (46)`

**Step 4: Verify line count**

Run: `wc -l SKILL.md`
Expected: ≤ 250 (must stay under!)

---

### Task 9: Update install.sh — 15 files

**Files:**
- Modify: `install.sh`

**Step 1: Update VERSION (line 14)**

Change: `VERSION="2.1.0"` → `VERSION="2.2.0"`

**Step 2: Update header text (line 19)**

Change "42 checks" → "46 checks"

**Step 3: Add new layer files to download loop (line 49)**

Add `FOUNDATION-EXTRA` and `QUALITY-PROD` to the for-loop:
```bash
for layer in SECURITY SECURITY-EXTRA FOUNDATION FOUNDATION-EXTRA QUALITY QUALITY-EXTRA QUALITY-PROD INTELLIGENCE CONTEXT DX DX-EXTRA MATURITY; do
```

**Step 4: Update verification (lines 62-68, 76)**

Add the 2 new files to the verification loop.
Change expected count: 13 → 15, "All 13 files" → "All 15 files".

---

### Task 10: Update Makefile — FILES list

**Files:**
- Modify: `Makefile`

**Step 1: Add new files to FILES variable (lines 3-10)**

Add `layers/FOUNDATION-EXTRA.md` and `layers/QUALITY-PROD.md` to the FILES list.

---

### Task 11: Update README.md — architecture, counts, install

**Files:**
- Modify: `README.md`

**Step 1: Update badges (line 7, 12)**

Change "42 automated checks" → "46 automated checks", badge count 42 → 46.

**Step 2: Update "At a Glance" (line 74)**

Change "42 checks" → "46 checks".

**Step 3: Update adaptive scoring table (lines 103-108)**

Starter: 17 → 19, Growing: 24 → 27, Mature: 30 → 34, Pro: 42 → 46.

**Step 4: Update layers table (lines 148-155)**

- Layer 0: 11 → 13, add "AI cost protection, backup strategy" to description
- Layer 1: 6 → 7, add "DB migrations" to description
- Layer 2: 11 → 12, add "error monitoring" to description

**Step 5: Update comparison table (line 87)**

Change `| Total checks | **42** |` → `| Total checks | **46** |`

**Step 6: Update installation section (line 226)**

Change "13 `.md` files" → "15 `.md` files".

**Step 7: Update manual install for-loop (line 243)**

Add FOUNDATION-EXTRA and QUALITY-PROD:
```bash
for f in SECURITY SECURITY-EXTRA FOUNDATION FOUNDATION-EXTRA QUALITY QUALITY-EXTRA QUALITY-PROD INTELLIGENCE CONTEXT DX DX-EXTRA MATURITY; do
```

---

### Task 12: Update CHANGELOG.md

**Files:**
- Modify: `CHANGELOG.md`

**Step 1: Add v2.2.0 entry at top**

```markdown
## v2.2.0 — Vibe Coder Essentials

### New checks (42 → 46)
- **0l** AI API cost protection `[core]` — billing alerts, max_tokens, dev/prod key separation
- **0m** Backup strategy `[advanced]` — managed DB backup detection, backup scripts
- **1g** Database migration tool `[quality]` — alembic/prisma/knex detection (only if DB exists)
- **2l** Error monitoring `[core]` — Sentry/LogRocket/Axiom detection, SENTRY_DSN in env

### New files
- **FOUNDATION-EXTRA.md** — advanced foundation checks (1g)
- **QUALITY-PROD.md** — production monitoring checks (2l)

### Improvements
- 3a agents: model field check (recommend haiku for read-only agents)
- 5a skills: allowed-tools check + launch.json detection
- Updated maturity matrix: Starter 19, Growing 27, Mature 34, Pro 46
- install.sh downloads 15 files (was 13)
```

---

### Task 13: Update CLAUDE.md — architecture section

**Files:**
- Modify: `CLAUDE.md`

**Step 1: Update architecture tree**

Add FOUNDATION-EXTRA.md and QUALITY-PROD.md to the tree. Update check counts.

---

### Task 14: Verify everything

**Step 1: Run make check**

Run: `make check`
Expected: 15/15 files OK

**Step 2: Run make lines**

Run: `make lines`
Expected: All within limits (SKILL.md ≤ 250, all others ≤ 400)

**Step 3: Grep for consistency**

```bash
# All files reference 46 checks:
grep -rn "42 check" *.md layers/*.md
# Should return 0 results (all updated to 46)

# Verify tag invariant: 19+8+7+12=46
grep -c '\[core\]' CHECKLIST.md    # expect ~19 (check lines only)
grep -c '\[quality\]' CHECKLIST.md # expect ~8
grep -c '\[advanced\]' CHECKLIST.md # expect ~7
grep -c '\[cc\]' CHECKLIST.md      # expect ~12
```

**Step 4: Commit**

```bash
git add -A
git commit -m "feat: v2.2.0 — 4 new checks (46 total), vibe coder essentials

New checks:
- 0l AI API cost protection [core]
- 0m Backup strategy [advanced]
- 1g Database migrations [quality]
- 2l Error monitoring [core]

New files: FOUNDATION-EXTRA.md, QUALITY-PROD.md
Improved: 3a model check, 5a allowed-tools + launch.json"
```
